[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "shared-secrets"
version = "0.1.0"
description = "Shared env/file secret loading for secure DB access gateway services"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["shared_secrets*"]